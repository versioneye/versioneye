<!DOCTYPE html>
<html>
	<head>
		<meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
	</head>
	<body>
		<p>Hello <%= @user.fullname %></p>
		<p>
			There are new releases out there. 
		</p>
		<p>
			<% count = 0 %>
			<table>				
				<% @notifications.each do |notification| %>
					<% product = Product.find_by_id(notification.product_id) %>
		      		<% version = product.get_version(notification.version_id) %>
					<tr style="background-color: <%= count % 2 == 0 ? 'beige' : 'white' %>">
						<td style="border-bottom: 1px dotted gray; padding: 5px;" >
							<a href="<%=@link%><%=product.to_param%>/version/<%=version.to_url_param%>"><%=product.name%></a>
						</td>
						<td style="border-bottom: 1px dotted gray; padding: 5px;" ><%= version.version %></td>
					</tr>
					<% notification.sent_email = true %>
					<% notification.save %>
					<% count = count + 1 %>
				<% end %>
			</table>
		</p>
		<p>
			We keep you informed. <br/>
			Have a great day!
		</p>
		<p>
			Your VersionEye Team
		</p>
	</body>
</html>

<div class="col-md-3" >
  <%= render 'settings/navi' %>
</div>

<div class="col-md-9" >

  <div class="content_header_def" >
    Teams
  </div>

  <div class="content" >

    <p>
      Here you can add/remove users to/from your teams.
      By assigning a team to one of your projects,
      all members of the team can see the project details,
      receive the project notification emails and can't modify/update the project with their own API key.
    </p>

    <p>
      </br>
      <%= form_for(:teams, :html => {:class => 'form-inline'} ) do |f|  %>
        <div class="form-group">
          <%= f.label :name, :class => 'control-label' %>
          <%= f.text_field :name, :class => "form-control" %>
          <button id="sign_in_button" type="submit" class="btn btn-big btn-primary" >Create New Team</button>
        </div>
      <% end %>
    </p>

    <br/>
    <br/>

    <table>
      <colgroup>
        <col width="75px;" />
        <col width="*" />
      </colgroup>

      <% @teams.each do |team| %>
        <% next if team.name.to_s.empty? %>

        <tr>
          <td>
            <%= form_tag( settings_teams_destroy_path(:id => team.ids) , :method => "post", :style => 'margin: 0;') do  %>
              <button type="submit" class="btn btn-danger" onClick="return confirmAction()" >Delete</button>
            <% end %>
          </td>
          <td>
            <%= link_to team.name, settings_teams_show_path(team) %>
          </td>
        </tr>

      <% end %>

    </table>

  </div>

</div>


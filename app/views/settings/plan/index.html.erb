<div class="span3" >
  <%= render 'settings/navi' %>
</div>

<div class="span8" >

  <div class="row content_header_def" >
    Plans &amp; Pricing - Manage your subscription here
  </div>

  <div class="span8 content" >
    <p>
      Open source projects are for free! We only charge a small fee for monitoring project files in private GitHub/Bitbucket repositories.<br/>
      <% if current_user.free_private_projects > 0 %>
        <br/>
        There are <b><%= current_user.free_private_projects %></b> private projects assigned to your account, for free!
      <% end %>
    </p>
    <hr />
    <%= render "pricing" %>
    <% if has_current_plan?( current_user ) %>
      <p>
        <br/>
        <form action="/settings/update_plan" method="POST" >
          <input  type="hidden" name="plan" value="<%= current_user.plan.name_id %>" />
          <button type="submit" class="btn btn-primary" id="update_cc_button" >Update Credit Card</button>
        </form>
      </p>
    <% end %>
  </div>

</div>

<% if product.icon.nil? %>
	<% if !product.language.nil? && !product.language.empty? %>
		<%= image_tag "language/#{product.language.downcase}.png" %>
	<% else %>
		<%= image_tag "eye_off.png" %>
	<% end %>									
<% else %>
	<img src="<%= product.icon %>" width="57" height="57"></img>
<% end %>
<% if icon.nil? %>
	<% if !language.nil? && !language.empty? %>
		<%= image_tag "language/#{language.downcase}.png" %>
	<% else %>
		<%= image_tag "eye_off.png" %>
	<% end %>									
<% else %>
	<img src="<%= icon %>" width="57" height="57"></img>
<% end %>
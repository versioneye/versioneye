<% user = audit.user %>
<b>
  <%= audit.created_at.strftime("%b %d, %Y - %H:%M") %>
  <% if user %>
    - <%= link_to user.fullname, user_path(user) %>
  <% end %>
</b> <br/>
<%= audit.action %> <br/>
<hr/>

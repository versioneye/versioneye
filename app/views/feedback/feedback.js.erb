cancel_button = document.getElementById("feedback_dialog_cancel");
if (cancel_button){
  // We do it sneaky like this because of a conflict on the API page. 
  cancel_button.click(); 
}
// jQuery('#dialog_feedback').dialog('close')
jQuery('#feedback').val('');
alert("Thank you very much for your feedback.");


<p>
  Add a project collaborator by adding his email address.
</p>

<div>
  <%= form_tag(add_collaborator_user_project_path(id: params[:id]),  class: "form-inline", style: "vertical-align:top;") do %>
    <div class="input-append">
      <input id="collaborator_email" name="collaborator[email]" class="span6 input" type="text" autocomplete="off" />
      <button class="btn btn-primary" type="submit" >
        Add collaborator
      </button>
    </div>
  <% end %>
</div>

<script>
  jQuery(document).ready(function(){
    console.debug("Collaborators autocomplete is initialized.");

    jQuery("input#collaborator_email").typeahead({
      name: "contacts",
      remote: {
        url: "/users/autocomplete?term=%QUERY"
      },
      engine: Hogan,
      template: [
        '<div>',
          '<p>',
            '<i class="icon-male">&nbsp;</i>',
            '<strong>{{ fullname }}</strong> ( {{ username }} )',
          '</p>',
        '</div>'
      ].join('')
    });
  });
</script>

<div class = "media">
  <%= render partial: "collaborator" , collection: @collaborators %>
</div>

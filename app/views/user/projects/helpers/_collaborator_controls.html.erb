<%= button_to(
      'Remove',
      user_collaboration_delete_path(collaboration_id: collaborator[:_id].to_s),
      :confirm => "Are you sure?",
      :class => "btn btn-mini btn-danger"
    )
%>
<a href = "<%= user_collaboration_path(id: collaborator[:_id].to_s) %>" class = "btn btn-mini btn-primary button_to">Configure</a>